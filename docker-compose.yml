version: '3.8'

services:
  web:
    build: .
    container_name: bookcircle_web
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
    restart: unless-stopped
